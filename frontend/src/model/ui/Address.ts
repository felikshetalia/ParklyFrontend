export type Address = {
  country: string;
  city: string;
  postalCode: string;
  streetName: string;
  streetNumber: string;
  lat: number;
  lon: number;
};

export type User = {
  name: string;
  surname: string;
  email: string;
  phoneNumber: string;
};
